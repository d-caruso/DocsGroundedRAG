name: keep-alive-supabase

on:
  schedule:
    - cron: "0 0 */6 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: psql "${{ secrets.SUPABASE_DB_URL }}" -c "SELECT 1;"
